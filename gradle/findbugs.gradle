apply plugin: 'findbugs'

findbugs {
    //Comment if you allow this tool to fail build
    ignoreFailures = true
    effort = 'min'
    reportLevel = 'high'
}

tasks.withType(FindBugs) {
    reports {
        xml.enabled false
        html.enabled true
    }
}