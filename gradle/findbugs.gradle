apply plugin: 'findbugs'

findbugs {
    ignoreFailures = true
    effort = 'min'
    reportLevel = 'high'
}

tasks.withType(FindBugs) {
    reports {
        xml.enabled false
        html.enabled true
    }
}